type Point = {
  x: number,
  y: number,
};

type Rect = {
  x: number,
  y: number,
  width: number,
  height: number,
};

type Vector = {
  x: number,
  y: number,
};

type LayerProperties = {
  x: number,
  y: number,
  width: number,
  height: number,
  backgroundColor?: string,
};
